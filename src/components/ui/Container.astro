---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  className?: string;
}

const { as = 'div', className = '' } = Astro.props as Props;
const Tag = as;
---

<Tag class={`mx-auto w-full max-w-6xl px-5 md:px-8 ${className}`}>
  <slot />
</Tag>
